System:
  mc - edit config
  nrs - rebuild and switch
  code-server
    Get the version, run this to get the hash...
    nix-prefetch-git https://github.com/coder/code-server.git --rev v4.98.2 --fetch-submodules
    Run this... then update the outputHash.
    nix-build -E 'with import <nixpkgs> {}; with pkgs.python3Packages; callPackage ./default.nix { jq = pkgs.jq; }'
User:
  ned:
    rustup default stable
    Clone ned repo.
    cd ned
    cargo build --release
